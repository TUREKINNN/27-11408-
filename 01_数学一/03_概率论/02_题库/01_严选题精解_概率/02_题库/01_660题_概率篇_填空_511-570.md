---
title: "660题 · 概率篇 · 填空题 511-570"
date: 2026-06-22
科目: 数学一
章节: 随机事件和概率 - 数理统计（填空题部分）
来源: 《基础过关660题》数学一·概率论与数理统计篇
难度: 基础~中等
题目数: 60（511-570）
状态: 已从 MinerU 提取题面+答案+解析
---

# 660题 · 概率篇 · 填空题（第 511-570 题）

>
> - 已从 MinerU 提取：150/150 题（含11题无题面）
> - 完整录入题面+答案+解析：约 53/60 道
> - **缺失题号**（题面OCR缺失，需用户视觉核对）：538, 539, 540, 547, 566, 567
> - **下一步**：用户拖图视觉核对缺失题

---

## 章节分布

| 章节 | 题号 |
|------|------|
| 一、随机事件和概率 | 511-520 |
| 二、一维随机变量及其分布 | 521-535 |
| 三、二维随机变量及其分布 | 536-548 |
| 四、随机变量的数字特征 | 549-560 |
| 五、大数定律与中心极限定理 | 561-563 |
| 六、数理统计 | 564-570 |

---

## 题目


### 一、随机事件和概率 (Q511-520)
> 题面：已知事件 $A, B$ 仅发生一个的概率为 0.3, 且 $P(A) + P(B) = 0.5$ , 则 $A, B$ 至少有一个不发生的概率为 ____.

> 题面：对事件 A, B, 已知 $P(A) = 0.6, P(B) = 0.7$ , 那么 $P(AB)$ 可能取到的最大的值是 ____, 可能取到的最小的值是 ____, $P(A \cup B)$ 可能取到的最大的值是 ____, 可能取到的最小的值是 ____.

> 题面：已知甲袋有 3 个白球, 6 个黑球, 乙袋有 5 个白球, 4 个黑球. 先从甲袋中任取一球放入乙袋, 然后再从乙袋中任取一球放回甲袋, 则甲袋中白球数不变的概率为 ____.

> 题面：独立地将一枚均匀硬币重复掷 5 次, 则正、反面都至少出现 2 次的概率为 ____.

> 题面：对事件 A, B, 已知 $P(A \cup B) = \frac{3}{5}$ , $P(\overline{A} \cup B) = \frac{9}{10}$ , 则 $P(B) =$ ____.

> 题面：设随机事件 A, B, C 满足 $A \subset C$ , $P(B) = P(C) = \frac{1}{3}$ ，且 B, C 相互独立，则 $P(A \cup B \mid \overline{C}) =$ ____.

> 题面：随机地抛掷甲、乙两个均匀骰子,则甲骰子出现的点数大于乙骰子出现的点数的概率 $p =$ ____.

> 题面：一射手对同一目标独立地进行四次射击. 若至少命中一次的概率为 $\frac{15}{16}$ , 则该射手对同一目标独立地进行四次射击中至少没命中一次的概率为 ____.

> 题面：已知人群中血型为 A, B, O, AB 型的人占比分别为 a, b, c, d ( $a + b + c + d = 1$ ). O 型血为“万能输血者”（即可以向任何血型的人输血），AB 型为“万能受血者”（即可以接受任何血型的人输血），其他情况则只有同型才能输血。现从人群中任选 1 人为输血者，再任选另 1 人为受血者，则输血配对成功的概率为 ____。

> 题面：设 $A, B$ 为随机事件, 已知 $P(A) = \frac{1}{4}, P(A \mid B) = \frac{1}{2}$ 和 $P(B \mid A) = \frac{1}{3}$ , 则 $P(A \cup B) = \_$ .


### 二、一维随机变量及其分布 (Q521-535)
> 题面：设随机变量 X 服从参数为 $\lambda$ 的指数分布, 对 X 作 3 次独立重复观察, 至少有一次观测值大于 2 的概率为 $\frac{7}{8}$ , 则 $\lambda =$ ____ .

> 题面：设随机变量 X 服从参数为 1 的指数分布, 则 $P\{3 > X > 2 \mid X > 1\} =$ ____.

> 题面：设随机变量 $X$ 的概率密度函数 $f(x)$ 满足 $f(1 + x) = f(1 - x)$ , 且 $\int_{1}^{2} f(x) \mathrm{d}x = 0.3$ , $X$ 的分布函数 $F(x)$ , 则有 $F(0) =$ ____.

> 题面：设随机变量 $X$ 服从区间 $(-1,3)$ 上的均匀分布， $Y = X^2$ ，则 $Y$ 的概率密度 $f_{Y}(y) =$

> 题面：设随机变量 $X \sim N(\mu, \sigma^2)(\sigma > 0)$ , 其分布函数为 $F(x)$ , 则有 $$ F (\mu + x \sigma) + F (\mu - x \sigma) = \underline {{{\quad}}}.

> 题面：设随机变量 $X$ 的概率密度函数 $f(x) = \begin{cases} x, & a < x < b, \\ 0, & \text{其他}, \end{cases} (a > 0)$ , 其中 $a, b$ 为待定常数, 且 $E(X^2) = 2$ , 则 $P\{|X| < \sqrt{2}\} =$ ____.

> 题面：设随机变量 X 的概率密度函数为 $f(x)=\left\{\begin{aligned}x\mathrm{e}^{-x},&x>0,\\ 0,&x\leqslant0,\end{aligned}\right.$ 则 $P\{X\leqslant2\mid X\geqslant1\}$ 的值为 ____.

> 题面：已知随机变量 $X$ 的概率分布为 $P\{X = k\} = \frac{1}{3} (k = 1,2,3)$ , 当 $X = k$ 时随机变量 $Y$ 在 $(0,k)$ 上服从均匀分布, 即 $$ P \{Y \leqslant y \mid X = k \} = \left\{ \begin{array}{l l} 0, & y \leqslant 0, \\ \frac {y}{k}, & 0 <   y <   k, \\ 1, & k \leqslant y, \end{array} \right.

> 题面：随机变量 $X$ 服从正态分布 $N(3,6^2), P\{3 < X < 4\} = 0.2$ ，则 $P\{X \geqslant 2\} =$ 多维随机变量及其分布

> 题面：随机变量 $(X, Y)$ 的分布律为： 设 $F(x,y)$ 为 $(X,Y)$ 的分布函数，则 $P\{0 < X + Y \leqslant 2.3\} = \_\_\_\_ , F(1,0) = \_\_\_\_ ,$ $P\{Y \leqslant -1\} = \_\_\_\_ ,$ 随机变量 $2X - Y$ 的分布律为 \_\_\_\_ , $P\{X \leqslant 1 \mid Y = 0\} =$

> 题面：甲袋中有2个白球,乙袋中有2个黑球,每次独立、随机地分别从这2个袋中任取1球交换放入对方袋中,这样交换取、放共3次.3次后,设甲袋中有X个白球,则X的分布律为____.

> 题面：已知随机变量 $(X, Y)$ 的概率密度函数 $f(x, y) = \begin{cases} e^{-y}, & 0 < x < y, \\ 0, & \text{其他}, \end{cases}$ 则 $P(X + Y \leqslant 1) =$ ____.

> 题面：设随机变量 $X_{1}$ 和 $X_{2}$ 相互独立，已知 $X_{1} \sim B\left(1, \frac{3}{4}\right), X_{2}$ 的分布函数为 $F(x)$ ，则随机变量 $Y = X_{1} + X_{2}$ 的分布函数 $F_{Y}(y) =$ ____.

> 题面：设随机变量 $X \sim N(0,1)$ , 给定 $X = x$ 的条件下随机变量 $Y$ 条件分布是 $N(\rho x, 1 - \rho^2)$ , 这里 $0 < \rho < 1$ . 给定 $X = x, Y = y$ 的条件下随机变量 $Z$ 的条件分布是 $N(\rho y, 1 - \rho^2)$ , 则 $(Y, Z)$ 的联合概率密度 $f_{(Y,Z)}(y,z) =$ ____.

> 题面：(续上题, 上一题如果做不出, 做此题可将上一题的结论当作“已知”) 追问:给定 Y = y 的条件下, X 的条件概率密度 $f_{X|Y}(x \mid y) =$ ____.


### 三、二维随机变量及其分布 (Q536-548)
> 题面：则 $P\{X^2Y^2 = 1\} =$ ____.

> 题面：设相互独立的两随机变量 $X, Y$ 均服从 $[0, 3]$ 上的均匀分布, 则 $P\{1 < \max(X, Y) \leqslant 2\}$ 的值为 ____. 538

> 题面：设 $(X,Y) \sim N(\mu_1, \mu_2; \sigma_1^2, \sigma_2^2; 0)$, 其分布函数为 $F(x,y)$, 已知 $F(\mu_1, y) = \dfrac{1}{4}$, 则 $y = $ ______
> > 由二维正态分布的边缘分布性质, $F_X(x) = F(x, +\infty) \sim N(\mu_1, \sigma_1^2)$.
>
> $F(\mu_1, y) = P(X \leq \mu_1, Y \leq y) = P(X \leq \mu_1) \cdot P(Y \leq y)$ (因 $X,Y$ 独立且 $\rho=0$)
>
> $= \frac{1}{2} \cdot F_Y(y) = \frac{1}{4}$
>
> 故 $F_Y(y) = \frac{1}{2}$, 即 $y = \mu_2$.

> 题面：设随机变量 $X$ 和 $Y$ 相互独立, 且 $X$ 服从标准正态分布, 其分布函数为 $\Phi(x)$, $Y$ 的概率分布为 $P(Y=-1) = P(Y=1) = \dfrac{1}{2}$, 则随机变量 $Z = XY$ 的分布函数 $F(x) = $ ______
> > $Y$ 取 $\pm 1$ 各以 $\frac{1}{2}$ 概率, 故 $Z = XY$ 取 $X$ 或 $-X$, 概率各 $\frac{1}{2}$.
>
> $F_Z(x) = P(XY \leq x) = \frac{1}{2}P(X \leq x) + \frac{1}{2}P(-X \leq x)$
>
> $= \frac{1}{2}\Phi(x) + \frac{1}{2}[1 - \Phi(-x)]$
>
> 由正态分布对称性 $\Phi(-x) = 1 - \Phi(x)$:
>
> $F_Z(x) = \frac{1}{2}\Phi(x) + \frac{1}{2}\Phi(x) = \Phi(x)$
>
> 故 $Z = XY$ 也服从标准正态分布.

> 题面：随机变量 $(X,Y)$ 的概率密度为 $f(x,y) = \dfrac{1}{\pi}\exp\left[-\dfrac{1}{2}(2x^2 + 2xy + y^2 - 22x - 14y + 65)\right], (x,y) \in \mathbb{R}^2$, 则 $E(X) = $ ____, $D(X) = $ ____, $E(Y) = $ ____, $D(Y) = $ ____, $X, Y$ 的相关系数 $\rho_{XY} = $ ______
> > 配方法化为标准二维正态形式.
>
> 设 $\begin{pmatrix} X \\ Y \end{pmatrix} \sim N(\boldsymbol{\mu}, \boldsymbol{\Sigma})$, 其中 $\boldsymbol{\Sigma}^{-1} = \begin{pmatrix} 2 & 1 \\ 1 & 1 \end{pmatrix}$.
>
> 求 $\boldsymbol{\Sigma} = \begin{pmatrix} 2 & 1 \\ 1 & 1 \end{pmatrix}^{-1} = \begin{pmatrix} 1 & -1 \\ -1 & 2 \end{pmatrix}$.
>
> 由线性项 $-22x - 14y$, 计算 $\boldsymbol{\mu} = \boldsymbol{\Sigma} \begin{pmatrix} 11 \\ 7 \end{pmatrix} = \begin{pmatrix} 1 & -1 \\ -1 & 2 \end{pmatrix} \begin{pmatrix} 11 \\ 7 \end{pmatrix} = \begin{pmatrix} 4 \\ 3 \end{pmatrix}$.
>
> 由 $\boldsymbol{\Sigma} = \begin{pmatrix} 1 & -1 \\ -1 & 2 \end{pmatrix}$: $D(X) = 1$, $D(Y) = 2$, $\text{Cov}(X,Y) = -1$.
>
> $\rho_{XY} = \dfrac{-1}{\sqrt{1 \cdot 2}} = -\dfrac{1}{\sqrt{2}}$.
>
> 故 $E(X)=4, D(X)=1, E(Y)=3, D(Y)=2, \rho_{XY}=-\dfrac{1}{\sqrt{2}}$.

> 题面：设随机变量 X 的分布律为 $P\{X=k\}=\frac{1}{2^{k}k!(\sqrt{e}-1)}, k=1,2,\cdots$ ，则 X 的数学期望 $E(X)=$ ____.

> 题面：设随机变量 X 服从参数为 3 的泊松分布, $Y \sim N(-1, 2)$ 且 $D(X + Y) = 1$ , 则 X 与 Y 的相关系数 $\rho =$ ____ .

> 题面：设随机变量 X 服从参数为 2 的泊松分布, 则 $E(|X - E(X)|) =$ ____.

> 题面：甲袋中有 $a$ 白 $b$ 红共 $a + b$ 只球, 乙袋中有 $u$ 白 $v$ 红共 $u + v$ 只球. 现从甲袋中随机取 $c$ 只球 $(c \leqslant a + b)$ 放入乙袋, 然后从乙袋中任取 1 只球, 取得的这只球为白球的概率为 ____.

> 题面：设随机变量 $X_{1}, X_{2}, \cdots, X_{n} (n > 1)$ 独立同分布，且方差为 $\sigma^{2} > 0$ ，记 $Y_{1} = \sum_{i=2}^{n} X_{i}$ 和 $Y_{n} = \sum_{j=1}^{n-1} X_{j}$ ，则 $Y_{1}$ 和 $Y_{n}$ 的协方差 $\mathrm{Cov}(Y_{1}, Y_{n}) =$ ____.

> 题面：相互独立的随机变量 $X_{1}, X_{2}, \cdots, X_{n}$ 具有相同的方差 $\sigma^{2} > 0$ ，记 $\overline{X} = \frac{1}{n} \sum_{i=1}^{n} X_{i}$ ，则 $D(X_{1} - \overline{X}) =$ ____. 547

> 题面：设随机变量 $X$ 服从分布 $E(1)$, 记 $Y = \min\{X, 1\}$, 则 $Y$ 的数学期望 $E(Y) = $ ______
> > $X \sim E(1)$ 即参数为 1 的指数分布, 密度 $f(x) = e^{-x}$ (当 $x \geq 0$).
>
> $Y = \min\{X, 1\}$, $Y \in [0, 1]$.
>
> $E(Y) = \int_0^1 y f(y) dy + 1 \cdot P(X \geq 1) = \int_0^1 y e^{-y} dy + e^{-1}$
>
> $\int_0^1 y e^{-y} dy = [-y e^{-y}]_0^1 + \int_0^1 e^{-y} dy = -e^{-1} + [1 - e^{-1}] = 1 - 2e^{-1}$
>
> 故 $E(Y) = 1 - 2e^{-1} + e^{-1} = 1 - e^{-1}$.

> 题面：设随机变量 $X \sim U(0,3)$ , 随机变量 $Y$ 服从参数为 3 的泊松分布, 且 $X$ 与 $Y$ 的协方差为 -2, 则 $D(2X - 3Y + 4) =$ ____.


### 四、随机变量的数字特征 (Q549-560)
> 题面：相互独立的随机变量 $X_{1}$ 和 $X_{2}$ 均服从正态分布 $N\left(0, \frac{1}{2}\right)$ , 则 $D(|X_{1} - X_{2}|) =$

> 题面：设二维随机变量 $(X, Y)$ 的概率密度为 $$ f (x, y) = \left\{ \begin{array}{l l} {\mathrm{e} ^ {- x},} & {x > 0, 1 > y > 0,} \\ {0,} & {\text {其他},} \end{array} \right.

> 题面：设随机变量 $X$ 在 $[-1, b]$ 上服从均匀分布, 其中 $b$ 是未知常数, 根据切比雪夫不等式有 $P\{|X - 1| \geqslant \varepsilon\} \leqslant \frac{1}{3}$ , 则 $\varepsilon =$ ____.

> 题面：将一个骰子重复掷 n 次, 每次掷出的点数依次为 $X_{1}, \cdots, X_{n}$ . 则当 $n \to \infty$ 时, $\overline{X} = \frac{1}{n} \sum_{i=1}^{n} X_{i}$ 依概率收敛于 ____.

> 题面：设随机变量 $X_{1}, X_{2}, \cdots, X_{2n}, \cdots$ 独立且均服从指数分布 $E(\lambda)$ ，记 $Z_{i} = X_{2i} - X_{2i-1}, i = 1, 2, 3, \cdots$ ，则 $\sum_{i=1}^{n} Z_{i}$ 近似服从正态分布 $N(\_, \_)$ .

> 题面：利用中心极限定理, $\lim_{n\to\infty}\sum_{k=0}^{n}\frac{n^{k}}{k!}e^{-n}=$ ____.

> 题面：某车间有 200 台车床, 在生产期间内由于各种原因需停工. 已知开工率为 0.6 , 且各台车床停工与否相互独立. 而开工的车床需用电 $1 \mathrm{kW}$ , 应供该车间至少 ____ kW 电力才能以 $99.9 \%$ 的可能性保证不会因供电不足而影响生产. (用中心极限定理作近似计算, $\Phi(3.1) = 0.999$ , 这儿 $\Phi(\cdot)$ 为标准正态分布的分布函数, 结果用进一法保留一位小数)

> 题面：设相互独立的随机变量 $X_{1}, X_{2}, \cdots, X_{n}$ 均服从标准正态分布，记 $\overline{X} = \frac{1}{n} \sum_{i=1}^{n} X_{i}$ ，则随机变量 $X_{1} - \overline{X}$ 服从的分布及参数为 ____.

> 题面：设总体 X 服从区间 $[0,1]$ 上的均匀分布， $X_{1}, X_{2}, \cdots, X_{n}$ 为从 X 中抽得的简单随机样本， $Y = \min_{1 \leqslant i \leqslant n} X_{i}$ ，则 Y 的概率密度 $f_{Y}(y) =$ ____.

> 题面：设总体 X 的概率密度函数为 $f(x)=\frac{1}{2}\mathrm{e}^{-|x-\mu|}(-\infty<x<+\infty)$ , $X_{1}, X_{2}, \cdots, X_{n}$ 为总体 X 的简单随机样本, 其样本方差为 $S^{2}$ , 则 $E(S^{2})=$ ____.

> 题面：设总体 $X \sim F(n, n)$ , 且 $P\{|X| < A\} = 0.3, A$ 为一常数, 则 $P\left\{X < \frac{1}{A}\right\} =$ ____.

> 题面：总体 $X \sim N(\mu, \sigma^{2})(\sigma > 0)$ , $X_{1}, X_{2}, \cdots, X_{n}$ 为从 X 中抽得的简单随机样本, $\overline{X}$ 为样本均值, 则 $\frac{1}{\sigma^{2}} \sum_{i=1}^{n} (X_{i} - \mu)^{2} \sim$ ____ , $\frac{1}{\sigma^{2}} \sum_{i=1}^{n} (X_{i} - \overline{X})^{2} \sim$ ____. (写出分布缩写, 若有参数请注明)


### 五、大数定律与中心极限定理 (Q561-563)
> 题面：设随机变量 $X \sim t(n), Y \sim F(1, n)$ , 常数 $c$ 满足 $P\{X > c\} = 0.6$ , 则 $P\{Y > c^{2}\} =$ ____.

> 题面：$f(x) = Ae^{-x^2 + x}, x \in \mathbb{R}$ , 其中 $A$ 为待定常数. $f(x) = Ae^{-x^2 + x}, x \in \mathbb{R}$ , 其中 $A$ 为待定常数.

> 题面：设总体 $X \sim U[a, b]$ , $X_{1}, X_{2}, \cdots, X_{n}$ 为其简单随机样本，样本均值 $\overline{X}$ ，样本方差 $S^{2}$ ，则 设总体 $X \sim U[a, b]$ , $X_{1}, X_{2}, \cdots, X_{n}$ 为其简单随机样本，样本均值 $\overline{X}$ ，样本方差 $S^{2}$ ，则 a,b 的矩估计 $\hat{a}=$ ____, $\hat{b}=$ ____.


### 六、数理统计 (Q564-570)
> 题面：设总体 $X \sim U[a, b]$ , a, b 是未知参数, $X_{1}, X_{2}, \cdots, X_{n}$ 是总体 X 的简单随机样本, 则 设总体 $X \sim U[a, b]$ , a, b 是未知参数, $X_{1}, X_{2}, \cdots, X_{n}$ 是总体 X 的简单随机样本, 则 参数 a, b 的最大似然估计量分别为 ____.

> 题面：总体 $X$ 的概率分布为 X 0 1 2 P $\theta^{2}$ $2\theta(1-\theta)$ $(1-\theta)^{2}$ , $\theta \in \left(0, \frac{1}{2}\right)$ 为待估参数.

> 题面：设 $X_1, X_2, \cdots, X_n$ 是来自总体 $X$ 的简单随机样本, $X$ 的概率密度函数为 $f(x) = \begin{cases} \lambda^2 x e^{-\lambda x}, & x > 0, \\ 0, & x \leq 0, \end{cases}$ $\lambda > 0$, 则 $\lambda$ 的最大似然估计量 $\hat{\lambda} = $ ______
> > 似然函数 $L(\lambda) = \prod_{i=1}^n \lambda^2 x_i e^{-\lambda x_i} = \lambda^{2n} (\prod x_i) e^{-\lambda \sum x_i}$.
>
> 对数似然: $\ln L = 2n \ln \lambda + \sum \ln x_i - \lambda \sum x_i$.
>
> 求导: $\dfrac{\partial \ln L}{\partial \lambda} = \dfrac{2n}{\lambda} - \sum x_i = 0$.
>
> 解得 $\hat{\lambda} = \dfrac{2n}{\sum x_i} = \dfrac{2}{\overline{X}}$.
>
> 二阶导数 $< 0$, 故为最大值.

> 题面：设总体 $X$ 与 $Y$ 独立, $E(X) = \mu_1$, $E(Y) = \mu_2$, $D(X) = 1$, $D(Y) = 4$, 从 $X$ 和 $Y$ 中分别抽取简单随机样本: $X_1, X_2, \cdots, X_n$ 和 $Y_1, Y_2, \cdots, Y_m$, 记 $\overline{X}$ 和 $\overline{Y}$ 分别为这两组样本的样本均值, $\hat{\mu} = \overline{X} - \overline{Y}$ 为 $\mu_1 - \mu_2$ 的估计 (可验证 $\hat{\mu}$ 为 $\mu_1 - \mu_2$ 的无偏估计). 现 $n + m = 14$ 固定, 则当 $D(\hat{\mu})$ 最小时, $n = $ ______
> > $D(\hat{\mu}) = D(\overline{X} - \overline{Y}) = D(\overline{X}) + D(\overline{Y}) = \dfrac{1}{n} + \dfrac{4}{m}$.
>
> 要使 $D(\hat{\mu})$ 最小, 由 $m = 14 - n$:
>
> $D(\hat{\mu}) = \dfrac{1}{n} + \dfrac{4}{14-n} = f(n)$.
>
> 求导 $f'(n) = -\dfrac{1}{n^2} + \dfrac{4}{(14-n)^2} = 0$:
>
> $(14-n)^2 = 4n^2 \Rightarrow 14-n = 2n$ (取正) $\Rightarrow 3n = 14 \Rightarrow n = \dfrac{14}{3}$.
>
> 因 $n$ 必须为正整数, 比较 $n=4$ 与 $n=5$:
> - $n=4$: $D = 1/4 + 4/10 = 0.25 + 0.4 = 0.65$
> - $n=5$: $D = 1/5 + 4/9 = 0.2 + 0.444 = 0.644$
>
> 故 $n = 5$ 时 $D(\hat{\mu})$ 最小.

> 题面：设总体 $X \sim N(\mu, \sigma^{2})$ , $X_{1}, X_{2}, \cdots, X_{n}$ 是来自总体 X 的简单随机样本, 据此样本检测在检验水平 $\alpha$ 条件下的拒绝域范围会随 $\alpha$ 变大而变 ____.

> 题面：设 $x_{1}, x_{2}, \cdots, x_{n}$ 为来自总体 $N(\mu, \sigma^{2})$ 的样本值，其平均值 $\overline{x} = 9.0$ ，参数 $\mu$ 的置信度为 0.90 的双侧置信区间的置信下限为 7.8，则 $\mu$ 的置信度为 0.90 的双侧置信上限为 ____.

> 题面：设 $X_{1}, X_{2}, \cdots, X_{36}$ 是取自正态总体 $N(\mu, 0.04)$ 的简单随机样本，其中 $\mu$ 为未知参数，记 $\overline{X} = \frac{1}{36} \sum_{i=1}^{36} X_{i}$ ，如果对检验问题 $$ H _ {0}: \mu \leqslant 0. 5, H _ {1}: \mu > 0. 5

