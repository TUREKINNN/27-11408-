---
title: "408 易错点 · 算法对比集"
date: 2026-06-23
科目: 408 全科
类型: 算法对比
状态: 🔴 待用户补充
---

# 📕 408 易错点 · 算法对比集

> **建立时间**：2026-06-23
> **收录范围**：408 考试中容易记混的算法（时间/空间复杂度、稳定性等）
> **使用方法**：每发现一组易混算法，单独建一个条目

---

## 🎯 高频算法对比 TOP 10（占位）

> 以下是 408 考试中最高频的算法对比

<!--
### 1. 排序算法对比

| 算法 | 平均时间 | 最坏时间 | 空间 | 稳定性 | 适用场景 |
|:-----|:--------:|:--------:|:----:|:------:|:---------|
| 直接插入 | O(n²) | O(n²) | O(1) | ✅ | 数据基本有序 |
| 希尔排序 | O(n^1.3) | O(n²) | O(1) | ❌ | 中等规模 |
| 冒泡排序 | O(n²) | O(n²) | O(1) | ✅ | 教学用 |
| 快速排序 | O(n log n) | O(n²) | O(log n) | ❌ | 一般首选 |
| 简单选择 | O(n²) | O(n²) | O(1) | ❌ | 选择最少交换 |
| 堆排序 | O(n log n) | O(n log n) | O(1) | ❌ | 大数据量 |
| 归并排序 | O(n log n) | O(n log n) | O(n) | ✅ | 稳定排序 |
| 基数排序 | O(d(n+r)) | O(d(n+r)) | O(r) | ✅ | 整数/字符串 |

### 2. 查找算法对比

| 算法 | 平均时间 | 最坏时间 | 空间 | 适用场景 |
|:-----|:--------:|:--------:|:----:|:---------|
| 顺序查找 | O(n) | O(n) | O(1) | 无序/小数据 |
| 折半查找 | O(log n) | O(log n) | O(1) | 有序数组 |
| 分块查找 | O(√n) | O(n) | O(√n) | 部分有序 |
| 二叉排序树 | O(log n) | O(n) | O(n) | 动态集合 |
| 平衡二叉树 | O(log n) | O(log n) | O(n) | 动态集合保平衡 |
| B 树/B+ 树 | O(log n) | O(log n) | O(n) | 磁盘/数据库 |
| 哈希查找 | O(1) | O(n) | O(n) | 关键字分散 |

### 3. 图的遍历对比

| 算法 | 空间 | 适用 | 特点 |
|:-----|:----:|:----:|:-----|
| DFS 深度优先 | O(V) | 找路径/拓扑排序/连通分量 | 栈实现 |
| BFS 广度优先 | O(V) | 最短路径（无权图） | 队列实现 |

### 4. 最短路径算法对比

| 算法 | 时间复杂度 | 适用 | 特点 |
|:-----|:----------|:-----|:-----|
| Dijkstra | O(V²) 或 O((V+E)logV) | 单源、非负权 | 不能负权 |
| Floyd | O(V³) | 多源、可负权 | 任意两点 |
| Bellman-Ford | O(VE) | 单源、可负权 | 检测负环 |
| SPFA | 平均 O(E) | 单源、可负权 | Bellman-Ford 优化 |

### 5. 最小生成树算法对比

| 算法 | 时间复杂度 | 适用 | 特点 |
|:-----|:----------|:-----|:-----|
| Prim | O(V²) 或 O((V+E)logV) | 稠密图 | 选点 |
| Kruskal | O(E log E) | 稀疏图 | 选边 |

### 6. 进程调度算法对比

| 算法 | 特点 | 优点 | 缺点 |
|:-----|:-----|:-----|:-----|
| FCFS | 先来先服务 | 公平 | 平均周转长 |
| SJF | 短作业优先 | 平均周转短 | 长作业饥饿 |
| RR | 时间片轮转 | 响应快 | 上下文切换开销 |
| 优先级 | 按优先级调度 | 灵活 | 优先级反转/饥饿 |
| 多级反馈队列 | 综合 | 折中 | 实现复杂 |

### 7. 页面置换算法对比

| 算法 | 思想 | 性能 | 抖动 |
|:-----|:-----|:----:|:----:|
| OPT | 淘汰最远将来使用 | 最好（理论） | 不会 |
| FIFO | 先进先出 | 差 | 可能 |
| LRU | 最近最久未用 | 好 | 不会 |
| CLOCK | 时钟算法 | 折中 | 不会 |
| LFU | 最不经常使用 | 中 | 不会 |

### 8. 磁盘调度算法对比

| 算法 | 思想 | 优点 | 缺点 |
|:-----|:-----|:-----|:-----|
| FCFS | 先来先服务 | 公平 | 寻道长 |
| SSTF | 最短寻道优先 | 寻道短 | 饥饿 |
| SCAN | 电梯算法 | 兼顾 | 两端等待 |
| C-SCAN | 循环扫描 | 均匀 | 跨越磁道多 |

### 9. 数字签名 vs 消息认证

| 维度 | 数字签名 | 消息认证 |
|:-----|:---------|:---------|
| 目的 | 抗否认 | 完整性 + 认证 |
| 密钥 | 公私钥 | 对称密钥 |
| 速度 | 慢 | 快 |
| 用途 | 法律证据 | 普通通信 |

### 10. 对称加密 vs 非对称加密

| 维度 | 对称加密 | 非对称加密 |
|:-----|:---------|:-----------|
| 密钥 | 同一密钥 | 公钥 + 私钥 |
| 速度 | 快 | 慢 |
| 密钥分发 | 困难 | 简单 |
| 用途 | 加密数据 | 密钥交换/签名 |
| 例子 | AES, DES | RSA, ECC |
-->

---

## 📝 易混算法列表

<!--
### 【算法编号】A001

**📅 录入日期**：
**🏷️ 涉及科目**：（如：数据结构）
**⭐ 混淆频率**：（★★★）

---

#### 两个易混算法

- **算法 A**：
- **算法 B**：

#### 详细对比

| 维度 | 算法 A | 算法 B |
|:-----|:-------|:-------|
| 思想 | | |
| 时间复杂度 | | |
| 空间复杂度 | | |
| 稳定性 | | |
| 适用场景 | | |
| 代码复杂度 | | |

#### 典型例题

- 例题 1：（题号）
- 例题 2：（题号）

#### 记忆口诀

（用一句话区分两个算法）

#### 出处

- 笔记章节：§X.X
- 错题链接：（如有）

---
-->

---

## 📅 易混算法时间线

<!--
- [ ] 录入第 1 组易混算法
-->
