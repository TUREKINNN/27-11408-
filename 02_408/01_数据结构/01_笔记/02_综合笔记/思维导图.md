---
title: "思维导图"
tags: [考研, 11408, 408, 数据结构]
categories: 数据结构
  - 408
  - 数据结构
date: 2026-06-16
---

# 思维导图

> 本导图展示知识结构，配合 [[01_数学一/01_高等数学/01_笔记/09_数一笔记_高数/索引]] 使用

# 数据结构 知识体系思维导图

## 总览

```mermaid
mindmap
  root((数据结构))
    基本概念
      逻辑结构
        集合结构
        线性结构
        树形结构
        图状结构
      存储结构
        顺序存储
        链式存储
        索引存储
        散列存储
      算法复杂度
        时间复杂度
        空间复杂度
        最好/最坏/平均
    线性表
      顺序表
        定义与实现
        插入删除操作
        按值/按位查找
      单链表
        头插法/尾插法
        插入删除操作
        查找操作
      双链表
        结构定义
        插入删除操作
      循环链表
        循环单链表
        循环双链表
      静态链表
    栈和队列
      栈
        顺序栈
        链栈
        栈的应用
          括号匹配
          表达式求值
          递归转非递归
      队列
        顺序队列
        链式队列
        循环队列
        双端队列
        队列的应用
          BFS
          层次遍历
    树与二叉树
      二叉树
        性质与公式
        顺序存储
        链式存储
        遍历
          先序遍历
          中序遍历
          后序遍历
          层次遍历
        线索二叉树
        由遍历序列构造
      树与森林
        存储结构
        树与二叉树转换
        森林与二叉树转换
      哈夫曼树与编码
        构造方法
        哈夫曼编码
      并查集
    图
      图的存储
        邻接矩阵
        邻接表
        十字链表
        邻接多重表
      图的遍历
        深度优先搜索DFS
        广度优先搜索BFS
      最小生成树
        Prim算法
        Kruskal算法
      最短路径
        Dijkstra算法
        Floyd算法
      拓扑排序
      关键路径
    查找
      顺序查找
      折半查找
      分块查找
      二叉搜索树BST
      平衡二叉树AVL
        旋转操作
        平衡因子
      红黑树
      B树与B+树
        插入操作
        删除操作
        B树与B+树对比
      散列表
        散列函数
        冲突处理
          开放定址法
          链地址法
        查找效率分析
    排序
      插入排序
        直接插入排序
        折半插入排序
        希尔排序
      交换排序
        冒泡排序
        快速排序
      选择排序
        简单选择排序
        堆排序
      归并排序
      基数排序
      外部排序
        多路归并
        置换-选择排序
      排序算法比较
```

## 各章节详解

### 1. 基本概念

```mermaid
mindmap
  root((基本概念))
    数据
      数据元素
      数据项
      数据对象
    逻辑结构
      集合
      线性
      树形
      图状
    存储结构
      顺序
      链式
      索引
      散列
    算法特性
      有穷性
      确定性
      可行性
      输入
      输出
    复杂度分析
      时间复杂度
        O(1) 常数
        O(logn) 对数
        O(n) 线性
        O(nlogn) 线性对数
        O(n²) 平方
        O(2^n) 指数
      空间复杂度
      最好/最坏/平均
```

### 2. 线性表

```mermaid
mindmap
  root((线性表))
    顺序表
      静态分配
      动态分配
      插入 O(n)
      删除 O(n)
      按位查找 O(1)
      按值查找 O(n)
    单链表
      带头结点
      不带头结点
      头插法建表
      尾插法建表
      插入操作 O(n)
      删除操作 O(n)
      查找操作 O(n)
    双链表
      结构 prior data next
      插入四步
      删除两步
    循环链表
      循环单链表
      循环双链表
    静态链表
      数组模拟链表
      游标实现
    逆置问题
      头插法逆置
      就地逆置
    归并问题
      有序表合并
```

### 3. 栈和队列

```mermaid
mindmap
  root((栈和队列))
    栈
      特性 LIFO
      顺序栈
        top指针
        上溢下溢
        共享栈
      链栈
        不带头结点
      应用
        括号匹配
        表达式转换
          中缀转后缀
          后缀求值
        递归
          递归转非递归
        函数调用栈
    队列
      特性 FIFO
      顺序队列
        循环队列
          判空判满
          元素个数
      链式队列
        带头结点
        不带头结点
      双端队列
        输入受限
        输出受限
      应用
        层次遍历
        BFS
        操作系统调度
```

### 4. 树与二叉树

```mermaid
mindmap
  root((树与二叉树))
    二叉树
      定义与性质
        五条重要性质
        满二叉树
        完全二叉树
      存储
        顺序存储
        链式存储
      遍历
        先序 根左右
        中序 左根右
        后序 左右根
        层次遍历
        非递归实现
        由两种遍历构造
    线索二叉树
      中序线索化
      先序线索化
      后序线索化
      线索化过程
    树与森林
      存储
        双亲表示法
        孩子表示法
        孩子兄弟表示法
      转换
        树转二叉树
        森林转二叉树
        二叉树转树/森林
      遍历
        先根/后根
        层次遍历
    哈夫曼树
      构造过程
      WPL计算
      哈夫曼编码
      前缀编码
    并查集
      Find操作
      Union操作
      路径压缩
```

### 5. 图

```mermaid
mindmap
  root((图))
    基本概念
      有向图/无向图
      完全图
      连通图/强连通图
      度/入度/出度
    存储结构
      邻接矩阵
      邻接表
      十字链表
      邻接多重表
    遍历
      DFS深度优先
      BFS广度优先
      时间复杂度
    最小生成树
      Prim 适合稠密
      Kruskal 适合稀疏
      MST性质
    最短路径
      Dijkstra 单源最短
      Floyd 全源最短
      负权边问题
    拓扑排序
      AOV网
      排序过程
      判环
    关键路径
      AOE网
      关键活动
      最早最迟时间
```

### 6. 查找

```mermaid
mindmap
  root((查找))
    顺序查找
      ASL计算
      哨兵优化
    折半查找
      ASL计算
      判定树
      适用条件
    分块查找
      ASL计算
      块间有序块内无序
    二叉搜索树BST
      插入操作
      删除操作
      ASL
      最坏O(n)
    平衡二叉树AVL
      平衡因子
      LL旋转
      RR旋转
      LR旋转
      RL旋转
      插入调整
      删除调整
    红黑树
      五条性质
      插入调整
      删除调整
    B树与B+树
      B树定义
      B+树定义
      插入分裂
      删除合并
      对比区别
    散列表
      散列函数
        除留余数法
        直接定址法
      冲突处理
        开放定址法
          线性探测
          二次探测
          伪随机探测
        链地址法
      ASL计算
      装填因子
```

### 7. 排序

```mermaid
mindmap
  root((排序))
    插入排序
      直接插入排序
        稳定 O(n²)
      折半插入排序
        稳定 O(n²)
      希尔排序
        不稳定 O(n^1.3)
    交换排序
      冒泡排序
        稳定 O(n²)
      快速排序
        不稳定 O(nlogn)
        分治思想
        枢轴选取
    选择排序
      简单选择排序
        不稳定 O(n²)
      堆排序
        不稳定 O(nlogn)
        大根堆/小根堆
        建堆过程
        调整过程
    归并排序
      稳定 O(nlogn)
      二路归并
    基数排序
      稳定 O(d(n+r))
      最高位/最低位优先
    外部排序
      多路归并
      败者树
      置换-选择排序
      最佳归并树
    排序比较
      时间/空间/稳定性
      适用场景
