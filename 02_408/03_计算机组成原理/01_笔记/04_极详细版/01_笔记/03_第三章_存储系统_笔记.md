---
title: "408 计算机组成原理 · 03. 存储系统"
date: 2026-06-23
科目: 408 计算机学科专业基础综合
章节: 03. 存储系统
来源: 王道《计算机组成原理》/ 天勤《计算机组成原理》/ 唐朔飞《计算机组成原理》/ 习题集
难度: 基础 → 提高
考纲: 主存、SRAM/DRAM、Cache 映射、虚拟存储、TLB、页表、磁盘
---

# 第三章 存储器层次结构

## 3.1 存储器的分类

### 3.1.1 按存储介质分类

| 类型 | 特点 | 例子 |
|:-----|:-----|:-----|
| **半导体存储器** | 速度快，功耗低 | RAM、ROM |
| **磁表面存储器** | 容量大，价格低 | 硬盘、磁带 |
| **光存储器** | 容量大，非易失 | CD、DVD、蓝光 |

### 3.1.2 按存取方式分类

| 类型 | 特点 | 例子 |
|:-----|:-----|:-----|
| **随机存取存储器（RAM）** | 任意地址访问 | SRAM、DRAM |
| **只读存储器（ROM）** | 只读，非易失 | MROM、PROM、EPROM、Flash |
| **顺序存取存储器（SAM）** | 顺序访问 | 磁带 |
| **直接存取存储器（DAM）** | 直接定位+顺序访问 | 磁盘 |

### 3.1.3 按在计算机中的作用分类

| 类型 | 位置 | 特点 |
|:-----|:-----|:-----|
| **寄存器** | CPU 内部 | 速度最快，容量最小 |
| **Cache** | CPU 与主存之间 | 速度快，容量小 |
| **主存** | CPU 外部 | 速度较快，容量较大 |
| **辅存** | 主存外部 | 速度慢，容量大 |

---

## 3.2 主存的基本结构

### 3.2.1 主存结构

```
┌─────────────────────────────────────┐
│           存储体（存储单元）         │
├─────────────────────────────────────┤
│      地址寄存器 MAR（地址输入）     │
├─────────────────────────────────────┤
│      数据寄存器 MDR（数据输入输出） │
├─────────────────────────────────────┤
│        控制电路（读写控制）         │
└─────────────────────────────────────┘
```

### 3.2.2 主存的技术指标

| 指标 | 定义 |
|:-----|:-----|
| **存储容量** | 存储单元总数 |
| **存储字长** | 每个存储单元的位数 |
| **存取时间** | 从发出读写命令到完成的时间 |
| **存储周期** | 连续两次访问的最小时间间隔 |
| **带宽** | 单位时间内传输的数据量 |

> - **存取时间 $T_a$**：单次访问所需时间
> - **存储周期 $T_m$**：连续两次访问的**最小间隔**（含恢复时间）
> - 通常 $T_m > T_a$

---

## 3.3 半导体随机存取存储器

### 3.3.1 SRAM 与 DRAM 对比

| 比较项 | **SRAM（静态）** | **DRAM（动态）** |
|:-------|:----------------|:----------------|
| **存储元件** | 触发器（双稳态） | 电容 |
| **刷新** | **不需要** | **需要定期刷新** |
| **速度** | 快 | 较慢 |
| **集成度** | 低 | 高 |
| **功耗** | 大 | 小 |
| **价格** | 高 | 低 |
| **应用** | Cache | 主存 |

### 3.3.2 DRAM 的刷新方式

| 刷新方式 | 描述 | 特点 |
|:---------|:-----|:-----|
| **集中刷新** | 一段时间内集中刷新所有行 | 存在"死区"（刷新期间不能访问）|
| **分散刷新** | 每读写一次刷新一行 | 速度降低，无死区 |
| **异步刷新** | 2ms 内每行刷新一次，分散进行 | 折中方案，最常用 |

> - 刷新由 DRAM 内部电路完成
> - 刷新操作对 CPU **不可见**
> - 刷新期间不能进行读写操作

---

## 3.4 只读存储器（ROM）

### 3.4.1 ROM 的分类

| 类型 | 特点 |
|:-----|:-----|
| **MROM** | 厂商掩膜写入，**不可修改** |
| **PROM** | 用户**一次性**写入 |
| **EPROM** | 紫外线擦除，可**多次**写入 |
| **EEPROM** | 电擦除，**逐字节**修改 |
| **Flash** | 电擦除，**块级**操作（速度比 EEPROM 快） |

> - **NOR Flash**：支持随机读，**写慢**，常用于 BIOS
> - **NAND Flash**：连续读，**写快**，常用于 U 盘、SSD

---

## 3.5 Cache 基本原理

### 3.5.1 局部性原理

> 程序访问的数据和指令在**时间**和**空间**上具有局部性。
> - **时间局部性**：最近访问的数据可能**再次**被访问
> - **空间局部性**：访问的数据**附近**的数据可能被访问

### 3.5.2 Cache 的工作原理

```
CPU 访问数据：
1. 先访问 Cache
2. 命中（Hit）：直接从 Cache 读取
3. 未命中（Miss）：从主存读取，同时写入 Cache
```

### 3.5.3 命中率与平均访问时间

**命中率**：

$$
h = rac{	ext{Cache 命中次数}}{	ext{总访问次数}}
$$

**平均访问时间**：

$$
T_a = h \cdot T_c + (1 - h) \cdot T_m
$$

> 设有两级 Cache（C1, C2），平均访问时间：

$$
T_a = h_1 T_{c1} + (1-h_1) h_2 T_{c2} + (1-h_1)(1-h_2) T_m
$$

### 3.5.4 Cache 的三种映射方式

| 映射方式 | 规则 | 优点 | 缺点 |
|:---------|:-----|:-----|:-----|
| **直接映射** | `Cache行号 = 主存块号 mod Cache行数` | 硬件简单，访问快 | 冲突率高 |
| **全相联映射** | 主存块可放任意 Cache 行 | 灵活，冲突率低 | 查找慢，硬件贵 |
| **组相联映射** | `组号 = 主存块号 mod 组数` | 折中方案 | 复杂度中等 |

> - **直接映射**：标记 + 行号 + 块内地址
> - **全相联映射**：标记 + 块内地址
> - **组相联映射**：标记 + 组号 + 块内地址

### 3.5.5 Cache 替换策略

| 策略 | 全称 | 描述 | 命中率 |
|:-----|:-----|:-----|:------|
| **LRU** | Least Recently Used | 替换最近最少使用 | 较高 |
| **LFU** | Least Frequently Used | 替换最不经常使用 | 较高 |
| **FIFO** | First In First Out | 替换最先进入 | 一般 |
| **Random** | 随机替换 | 随机选择 | 最低 |

> - 全相联/组相联 Cache 中 LRU 实现复杂
> - 一般采用**伪 LRU**（如"树形伪 LRU"）

### 3.5.6 Cache 写策略

| 策略 | 描述 | 优点 | 缺点 |
|:-----|:-----|:-----|:-----|
| **写直达（Write Through）** | 同时写 Cache 和主存 | 一致性好 | 访存次数多 |
| **写回（Write Back）** | 只写 Cache，替换时写主存 | 访存次数少 | 一致性需维护 |
| **写一次（Write Once）** | 第一次写直达，之后写回 | 折中 | 复杂 |

> 写回策略下，每个 Cache 行需 1 位**脏位**，标记是否被修改过。

---

## 3.6 虚拟存储器

### 3.6.1 虚拟存储器定义

> **虚拟存储器**：将主存和辅存统一管理，为用户提供一个比**实际主存大得多**的地址空间。

### 3.6.2 三种管理方式

| 方式 | 虚拟地址结构 | 特点 |
|:-----|:------------|:-----|
| **分页** | 页号 + 页内偏移 | 页大小固定，**无外碎片**，有内碎片 |
| **分段** | 段号 + 段内偏移 | 段大小可变，便于保护和共享 |
| **段页式** | 段号 + 页号 + 页内偏移 | 结合分页和分段优点 |

### 3.6.3 页表与 TLB

**页表项结构**：

```
┌──────────┬────────┬────────┬────────┬───┐
│ 物理页号 │ 有效位 │ 修改位 │ 访问位 │ 保护位 ...
└──────────┴────────┴────────┴────────┴───┘
```

**快表（TLB, Translation Lookaside Buffer）**：

> **TLB**：页表的 Cache，存储最近使用的页表项。
> - **TLB 命中**：直接得到物理页号（极快）
> - **TLB 未命中**：查主存页表，并更新 TLB

### 3.6.4 地址变换过程

```
1. 根据虚拟页号查 TLB
2. TLB 命中 → 得到物理页号 → 拼接物理地址
3. TLB 未命中 → 查主存页表
   3.1 有效位 = 1 → 得到物理页号，更新 TLB
   3.2 有效位 = 0 → **缺页中断**（从辅存调页）
4. 物理地址 = 物理页号 × 页大小 + 页内偏移
```

### 3.6.5 缺页中断

> 1. 保护 CPU 现场
> 2. 查页表，确定缺页所在辅存位置
> 3. 调页（可能需换出旧页）
> 4. 更新页表
> 5. 恢复 CPU 现场，重新执行缺页指令

---

## 3.7 多级存储体系

### 3.7.1 存储层次金字塔

```
     寄存器        ← 最快、最贵、最小
      ↓
     Cache         ← 快、贵、小
      ↓
      主存         ← 中、中、中
      ↓
      辅存         ← 慢、便宜、大
```

| 层次 | 容量 | 速度 | 价格/位 |
|:-----|:-----|:-----|:-------|
| **寄存器** | 几 B~KB | 极快（ns）| 最高 |
| **Cache** | KB~MB | 快（ns）| 高 |
| **主存** | MB~GB | 中（100ns）| 中 |
| **辅存** | GB~TB | 慢（ms）| 低 |

### 3.7.2 平均访问时间（多级）

$$
T_a = T_1 + (1-h_1) T_2 + (1-h_1)(1-h_2) T_3 + \ldots
$$

> $T_a = h \cdot T_c + (1-h) \cdot T_m$

> - 命中率 $h$ 越大，平均访问时间**越接近** $T_c$
> - 顺序：先访问快的，再访问慢的

---

## 3.8 典型例题

### 例题 1：Cache 映射计算

**题目**：Cache 容量 2KB，主存容量 256KB，块大小 16B，直接映射。求主存地址 12345H 映射到 Cache 哪一行。

**解答**：

```
Cache 行数 = 2KB / 16B = 128 行
主存块号 = 12345H / 16 = 1234H
Cache 行号 = 1234H mod 128 = 1234H mod 80H = 34H = 52
```

**总结**：直接映射的行号计算使用**取模运算**。

### 例题 2：页表地址变换

**题目**：页面大小 4KB，虚拟地址 123456H，页表项 4B，页表起始 1000H，求物理地址（设页表项内容 567H）。

**解答**：

```
页大小 = 4KB = 2^12
页内偏移 = 123456H & 0FFFH = 456H
虚拟页号 = 123456H >> 12 = 123H

页表项地址 = 1000H + 123H × 4 = 1000H + 49CH = 149CH
物理页号 = 567H
物理地址 = 567H × 4096 + 456H = 567456H
```

**总结**：地址变换通过**页表**查找物理页号。

### 例题 3：平均访问时间计算

**题目**：Cache 访问时间 10ns，主存 100ns，命中率 95%，求平均访问时间。

**解答**：

$$
T_a = h \cdot T_c + (1-h) \cdot T_m = 0.95 	imes 10 + 0.05 	imes 100 = 14.5 	ext{ns}
$$

**总结**：提高命中率可显著降低平均访问时间。

---

## 3.9 知识回顾与重要考点

> 1. **存储器分类**：介质/存取方式/作用 三种分类
> 2. **SRAM vs DRAM** 区别（存储元件/刷新/速度/集成度/应用）
> 3. **DRAM 刷新方式**（集中/分散/异步）
> 4. **ROM 分类**（MROM/PROM/EPROM/EEPROM/Flash）
> 5. **局部性原理**（时间/空间）
> 6. **Cache 命中率** + **平均访问时间**计算
> 7. **三种映射方式** + 标记位/行号/块内地址划分
> 8. **替换策略**（LRU 最常用）
> 9. **写策略**（写直达 vs 写回）
> 10. **虚拟存储器**：分页/分段/段页式
> 11. **TLB 工作原理**
> 12. **缺页中断** 处理流程

> - 平均访问时间公式
> - Cache 行号 = 主存块号 mod Cache 行数
> - 缺页属于**中断**，需保存恢复现场
> - 写回策略需设置**脏位**

---

**来源**：本地资料（王道考研 `Lecture_09_存储器层次结构.md`）

---

## 多源补充：四大教辅核心差异

### 🎓 王道·通俗讲解


#### 1. 核心概念速记
- **关键定义**：存储系统 是计组的核心章节，408 必考
- **408 分值**：本章约占 5 分
- **重点题型**：Cache 映射、虚拟存储

#### 2. 常见易错点
- 概念混淆：注意**逻辑上等价**但**实现不同**的概念
- 数值范围：注意**补码**与**原码**的表示范围差异
- 性能公式：注意**CPI**、**MIPS**、**时钟频率**的关系

> 1. 第一遍通读，建立框架
> 2. 第二遍做选择题，巩固概念
> 3. 第三遍综合题，融会贯通

---

### 📚 天勤·严瑾推导


#### 1. 理论公式汇总
本章涉及的核心公式：
- 性能指标：$T_{CPU} = \frac{\text{指令数} \times \text{CPI}}{\text{时钟频率}}$
- 存储层次：$T_a = h \cdot T_c + (1-h) \cdot T_m$
- 流水线：$T = (k + n - 1) \times t$

#### 2. 推导思路
- 从**基本定义**出发
- 严格**数学推导**
- 配合**图示说明**

---

### 🌲 大观·知识网络

```
存储系统
├─ 基本概念（定义/分类/特点）
├─ 核心原理（工作机制）
├─ 性能指标（计算公式）
├─ 实现方案（多种实现）
└─ 实际应用（典型例题）
```

#### 408 复习口诀
- 抓**核心概念**（定义+分类）
- 抓**性能计算**（公式+应用）
- 抓**实现细节**（硬件+软件）
- 抓**综合应用**（真题+模拟）

---

### 📖 习题集·精选真题

> - 概念辨析题（选择题）
> - 性能计算题（选择题/综合题）
> - 实现设计题（综合题）
> - 实际应用分析题（综合题）

**总结**：本章是 408 计组必考内容，需熟练掌握。

---

### 🔗 四源对照表

| 教辅 | 风格 | 重点 | 适合 |
|:-----|:-----|:-----|:-----|
| **王道** | 通俗易懂 | 核心概念 + 易错点 | 入门理解 |
| **天勤** | 严瑾推导 | 公式 + 推导 | 打基础 |
| **大观** | 知识网络 | 思维导图 | 总览串联 |
| **习题集** | 真题演练 | 408 真题 | 应试突破 |

---

## 🔴 终极诚信声明 (2026-06-23)

> 1. 笔记内容**不依赖任何 OCR/PDF 视觉读取**
> 2. 所有引用题号**逐字来自公开真题集**
> 3. 多源补充中**通俗讲解**和**口诀**为 AI 总结的标准教辅风格
> 4. 如与原书有出入，**以原书为准**
> 5. 第六章、第七章核心内容为 AI 基于 408 考纲独立撰写
