---
title: "应用层"
tags: [考研, 11408, 408, 计算机网络, 应用层]
categories: 计算机网络
  - 408
  - 计算机网络
date: 2026-06-16
---

# 06-应用层

## 考情分析
- **选择题高频**：协议端口号、C/S与P2P对比、DNS解析过程、HTTP请求方法与状态码
- **大题考点**：DNS解析流程分析、HTTP请求/响应报文分析
- **易混点**：SMTP/POP3/IMAP区别、FTP主动被动模式、持久/非持久连接

---

## 一、C/S模型 vs P2P模型

| 特性 | C/S模型 | P2P模型 |
|------|---------|---------|
| **角色** | 服务器（提供服务）+ 客户端（请求服务） | 每个节点既是客户端又是服务器 |
| **服务器** | 固定IP，长期运行 | 无固定服务器 |
| **可扩展性** | 服务器是瓶颈 | 扩展性好，节点越多越快 |
| **典型应用** | Web、FTP、Email、DNS | BT下载、Skype |
| **管理** | 集中管理 | 分散管理 |
| **缺点** | 服务器单点故障 | 管理困难 |

---

## 二、DNS域名系统

### 2.1 域名空间（层次结构）
```
. （根域名，不写）
├── 顶级域名（TLD）
│   ├── 国家/地区：.cn .uk .jp
│   ├── 通用：.com .org .net .edu .gov
│   └── 反向：.arpa
├── 二级域名
│   ├── baidu.com
│   └── edu.cn
└── 三级域名
    └── www.baidu.com
```
- 域名从**左到右**层次递增（www.baidu.com → com是顶级）
- DNS使用**UDP**，端口**53**

### 2.2 DNS解析过程

**递归查询**（主机→本地DNS服务器）：
- 主机向本地DNS服务器查询，若本地无缓存，本地服务器**代替主机**完成整个查询

**迭代查询**（本地DNS→各级DNS服务器）：
1. 本地DNS → 根DNS："不知道，告诉你顶级域名服务器地址"
2. 本地DNS → 顶级DNS："不知道，告诉你权威DNS地址"
3. 本地DNS → 权威DNS："查到了，返回IP地址"

**完整流程**：
```
主机 → 本地DNS服务器（递归查询）
         → 根DNS服务器（迭代）
         → 顶级域DNS服务器（迭代）
         → 权威DNS服务器（迭代）
         ← 返回IP地址
       ← 返回给主机
```

### 2.3 DNS记录类型

| 记录类型 | 含义 | 示例 |
|---------|------|------|
| **A** | 域名→IPv4地址 | example.com → 93.184.216.34 |
| **AAAA** | 域名→IPv6地址 | |
| **CNAME** | 域名别名 | www.example.com → example.com |
| **MX** | 邮件交换记录 | 指定域名的邮件服务器 |
| **NS** | 域名服务器记录 | 指定域名由哪个DNS服务器解析 |
| **TXT** | 文本记录 | 用于SPF、验证等 |

---

## 三、FTP文件传输协议

### 基本特征
- 基于**TCP**
- 使用**C/S模型**
- 双连接：**控制连接** + **数据连接**

### 两种工作模式

| | 主动模式（Active） | 被动模式（Passive） |
|---|---|---|
| **控制连接** | 客户端 → 服务器 **端口21** | 同左 |
| **数据连接** | 服务器**端口20** → 客户端 | 客户端 → 服务器**随机端口** |
| **发起方** | 服务器发起数据连接 | 客户端发起数据连接 |

> 记忆：控制连接端口21全程存在，数据连接按需建立。
> 主动模式：服务器从20端口**主动**连客户端。
> 被动模式：服务器**被动**等客户端连过来。

---

## 四、电子邮件系统

### 4.1 系统架构
```
发件人 → 用户代理（UA）→ 发送方邮件服务器 → SMTP → 接收方邮件服务器 → 用户代理 → 收件人
```

### 4.2 SMTP（简单邮件传输协议）
- 端口：**25**
- 基于**TCP**
- 推（Push）协议：发送方→接收方
- 使用**7位ASCII**（不支持非ASCII内容）

**主要命令**：
| 命令 | 说明 |
|------|------|
| HELO | 发送方标识自己 |
| MAIL FROM: | 发件人地址 |
| RCPT TO: | 收件人地址 |
| DATA | 开始传输邮件内容 |
| QUIT | 结束会话 |

### 4.3 POP3（邮局协议第3版）
- 端口：**110**
- 基于**TCP**
- 拉（Pull）协议：用户代理从邮件服务器拉取邮件
- **下载并删除**或**下载并保留**

**主要命令**：
| 命令 | 说明 |
|------|------|
| USER | 用户名 |
| PASS | 密码 |
| RETR | 获取邮件 |
| DELE | 删除邮件 |
| QUIT | 退出 |

### 4.4 IMAP（因特网邮件访问协议）
- 端口：**143**
- 比POP3更强大，支持**在线管理**邮件
- 邮件保留在服务器上，可创建文件夹、搜索等
- 支持**部分获取**（如只获取邮件头）

### 4.5 MIME（通用因特网邮件扩充）
- 支持非ASCII内容（图片、音频、视频等附件）
- 在SMTP基础上扩展，通过**Base64**等编码将二进制转为ASCII

---

## 五、HTTP超文本传输协议

### 5.1 基本特征
- 基于**TCP**，端口**80**
- **无状态协议**（服务器不记录客户端状态，靠Cookie维持状态）
- 请求/响应模型

### 5.2 请求报文格式
```
请求行      方法 URL 版本\r\n
首部行      字段名: 值\r\n
            ...
空行        \r\n
实体主体    （POST时携带数据）
```

**请求方法**：
| 方法 | 说明 |
|------|------|
| **GET** | 请求获取资源（参数在URL中） |
| **POST** | 提交数据（参数在实体主体中） |
| **HEAD** | 只获取首部，不返回实体 |
| PUT | 上传资源 |
| DELETE | 删除资源 |

### 5.3 响应报文格式
```
状态行      版本 状态码 短语\r\n
首部行      字段名: 值\r\n
空行        \r\n
实体主体    （响应数据）
```

**常见状态码**：
| 状态码 | 含义 | 说明 |
|--------|------|------|
| **200** | OK | 请求成功 |
| **301** | Moved Permanently | 永久重定向 |
| **302** | Found | 临时重定向 |
| **304** | Not Modified | 资源未修改，用缓存 |
| **400** | Bad Request | 请求语法错误 |
| **403** | Forbidden | 服务器拒绝 |
| **404** | Not Found | 资源不存在 |
| **500** | Internal Server Error | 服务器内部错误 |
| **502** | Bad Gateway | 网关错误 |

### 5.4 持久连接 vs 非持久连接

| | 非持久连接 | 持久连接（Keep-Alive） |
|---|---|---|
| **TCP连接** | 每个请求对象建立单独TCP连接 | 同一TCP连接上发送多个请求 |
| **HTTP版本** | 默认HTTP/1.0 | HTTP/1.1默认持久连接 |
| **效率** | 低（TCP握手开销×N） | 高（复用连接） |
| **流水线** | 不支持 | 支持（不等响应就发下一请求） |

> 获取一个含10个图片的网页：非持久需11个TCP连接，持久只需1个。

### 5.5 HTTP vs HTTPS

| | HTTP | HTTPS |
|---|---|---|
| **端口** | 80 | **443** |
| **加密** | 明文传输 | **TLS/SSL加密** |
| **证书** | 不需要 | 需要CA证书 |
| **安全性** | 低 | 高 |

HTTPS = HTTP + TLS/SSL，加密对称密钥通过非对称加密协商。

### 5.6 Cookie工作原理
1. 客户端首次访问服务器
2. 服务器生成一个**唯一标识**，通过响应报文的`Set-Cookie`首部发给客户端
3. 客户端保存Cookie
4. 后续请求自动携带`Cookie`首部
5. 服务器通过Cookie识别用户身份

---

## 六、协议端口号速记

| 协议 | 端口 | 传输层 | 助记 |
|------|------|--------|------|
| HTTP | 80 | TCP | |
| HTTPS | 443 | TCP | |
| FTP控制 | 21 | TCP | |
| FTP数据 | 20 | TCP（主动） | |
| SMTP | 25 | TCP | 发邮件 |
| POP3 | 110 | TCP | 取邮件 |
| IMAP | 143 | TCP | 管邮件 |
| DNS | 53 | UDP/TCP | |
| DHCP | 67/68 | UDP | |

---

## 七、真题考法

1. **选择题**：判断协议使用的端口号或传输层协议
2. **选择题**：DNS解析过程的步骤判断
3. **大题**：分析HTTP请求/响应报文内容
4. **选择题**：FTP两种模式的区别

---

## 八、典型例题

### 例题1：DNS解析
> 一台主机要解析www.abc.com，本地DNS缓存中没有，递归查询的正确顺序是？

**解**：主机→本地DNS（递归），本地DNS→根DNS→com DNS→abc.com DNS（迭代），逐级返回。

### 例题2：端口号
> 下列协议中，使用TCP传输且端口号为25的是？
A. HTTP  B. FTP  C. SMTP  D. DNS

**解**：C。SMTP端口25，基于TCP。

### 例题3：HTTP
> 在浏览器输入URL后，使用非持久连接获取一个HTML文件和5个图片，共需几次TCP连接？

**解**：非持久连接每个对象单独建TCP，共1+5=**6次**TCP连接。

### 例题4：Cookie
> HTTP是无状态协议，网站如何记住用户登录状态？

**解**：通过Cookie机制。首次登录时服务器发Set-Cookie，浏览器后续请求自动携带Cookie标识身份。

---

## 九、易错点
1. DNS通常用**UDP**端口53（区域传送用TCP）
2. FTP的**控制连接**始终是端口21，**数据连接**主动模式用20，被动模式用随机端口
3. SMTP是**推**协议（发），POP3/IMAP是**拉**协议（收）
4. HTTP/1.1默认**持久连接**，HTTP/1.0默认非持久
5. Cookie存在**客户端**，Session存在**服务器端**
6. HTTPS = HTTP + TLS/SSL，端口443不是80

---

## 十、关联知识点
- [[02_408/04_计算机网络/01_笔记/03_综合笔记/05-传输层]] — 应用层协议基于TCP/UDP
- [[02_408/04_计算机网络/01_笔记/03_综合笔记/04-网络层]] — DNS解析涉及IP地址查找
- [[02_408/04_计算机网络/01_笔记/03_综合笔记/03-数据链路层]] — HTTP数据最终在链路层封装传输

---

## 补充内容

> 📖 **来源**：王道考研26版计网基础考点讲解.pdf

### 应用层功能概述

应用层对应用程序的通信提供服务。

**应用层功能**：
- 文件传输、访问和管理（FTP、TFTP）
- 电子邮件（SMTP、POP3）
- 虚拟终端（HTTP）
- 查询服务和远程作业登录（DNS）

### 网络应用模型的补充

**客户/服务器模型（C/S）**：
- **服务器**：提供计算服务的设备（永久提供服务、永久性访问地址/域名）
- **客户机**：请求计算服务的主机
  - 与服务器通信，使用服务器提供的服务
  - 间歇性接入网络
  - 可能使用动态IP地址
  - 不与其他客户机直接通信
- **应用**：Web、文件传输FTP、远程登录、电子邮件
- **特点**：
  1. 客户、服务器地位不平等
  2. 客户之间不直接通信
  3. 如果服务器损坏，影响全局
  4. 服务器负载较大

**P2P模型**：
- **特点**：
  1. 不存在永远在线的服务器（主机之间地位平等）
  2. 每个主机既可以**提供服务**，也可以**请求服务**
  3. 任意端系统/节点之间可以**直接通讯**
  4. 节点间歇性接入网络
  5. 节点可能改变IP地址
  6. 可扩展性好
  7. 网络健壮性强，单个节点损坏不影响全局
  8. 各节点可以分摊负载
- **应用**：BT种子下载

### DNS域名系统的补充

**域名结构**：
- **顶级域名**：
  - 国家顶级域名：cn、us、uk
  - 通用顶级域名：com、net、org、gov、int、aero、museum、travel
  - 基础结构域名/反向域名：arpa
- **二级域名**：
  - 类别域名：ac、com、edu、gov、mil、net、org
  - 行政区域名：用于我国各省、自治区、直辖市（如bj、js）
  - 也可以自己注册
- **三级域名**：www、mail

**域名服务器**：
1. **根域名服务器**：知道所有顶级域名服务器的域名、IP地址
2. **顶级域名服务器**：管理该顶级域名服务器注册的所有二级域名
3. **权限域名服务器**：负责一个区的域名服务器
4. **本地域名服务器**：本地域名服务器缓存近期被查询过的域名→IP，当一个主机发出DNS查询请求时，这个查询请求报文就发给本地域名服务器

**域名解析过程**：
- 主机查询本地域名服务器，**递归查询方式**
- 本地域名服务器向其他域名服务器采用**递归查询或迭代查询**

**递归查询**：若主机所询问的本地域名服务器不知道被查询域名的IP地址，则本地域名服务器就以DNS客户的身份，向根域名服务器继续发出查询请求报文（即替该主机继续查询），而不是让该主机自己进行下一步的查询。**给根域名服务器造成的负载过大**。

**迭代查询**：当根域名服务器收到本地域名服务器发出的迭代查询请求报文时，要么给出所要查询的IP地址，要么告诉本地域名服务器："你下一步应当向哪个顶级域名服务器进行查询"。然后让本地域名服务器进行后续的查询。同样，顶级域名服务器收到查询报文后，要么给出所要查询的IP地址，要么告诉本地域名服务器下一步应当向哪个权限域名服务器查询。最后，知道了所要解析的域名的IP地址后，把这个结果返回给发起查询的主机。

**高速缓存**：
- 为提高DNS的查询效率，并减少因特网上的DNS查询报文数量，在域名服务器中使用高速缓存，用来缓存最近查询过的域名的相关映射信息
- 因为主机名和IP地址之间的映射不是永久的，所以DNS服务器将在一段时间后丢弃高速缓存中的信息
- 在主机中同样也很需要高速缓存，许多主机在启动时从本地域名服务器下载域名和地址的全部数据库，维护存放自己最近使用的域名的高速缓存，并且只在从缓存中找不到域名时才使用域名服务器的高速缓存

**主机如何知道本地域名服务器的IP地址**：DHCP协议，在主机动态分配的时候，会配置主机的默认网关、IP地址、子网掩码，并会告诉主机默认DNS服务器的IP地址。

**重要结论**：
- **一个以太网卡对应一个IP地址**，拥有多个以太网卡的主机有多个IP地址
- **一个主机可对应多个域名**
- **单个域名可对应多个IP地址/MAC地址**
- **单个域名可对应多个主机**
- **多个域名可指向同一个主机IP地址**
- **Internet上提供客户访问的主机一定要有IP地址，域名随意**
- **授权域名服务器：将其管辖的主机名→IP地址**

### FTP文件传输协议的补充

**FTP概述**：
- 作用：提供不同种类主机系统之间的文件传输能力（软、硬件体系都可以不同）
- FTP提供**交互式**的访问，**允许客户指明文件的类型和格式**，并允许文件具有存取权限
- **基于TCP的FTP和基于UDP的简单文件传送协议TFTP的特点**：
  - 如果要存取一个文件，就必须先获得一个本地的文件副本（即**复制整个文件**）
  - 如果要修改文件，只能对**文件的副本进行修改，然后再将修改后的文件副本传回到原节点**
- **FTP屏蔽了各计算机系统的细节，因而适合在异构网络中任意计算机之间传送文件**
- FTP是基于客户/服务器（C/S）的协议
- **一个FTP服务器可同时为多个客户提供服务**

**FTP的服务器进程**：由两大部分组成：一个**主进程**（负责接收新的请求），另有n个**从属进程**（负责单个请求）

**主进程工作步骤**：
1. 打开熟知端口（21），使客户端能够连接上
2. 等待客户进程发出连接请求
3. 启动从属进程处理客户进程发来的请求
4. 回到等待状态，继续接收其他客户进程发来的请求

**登录**：
- 需要输入ftp地址、用户名&密码
- 也可以进行匿名登录（不需要输入用户名&密码）
- "匿名FTP服务器"：该服务器主要提供文件拷贝功能，**不要求用户登记注册**，**也不用**取得FTP服务器的**授权**

**工作原理**：
- 在进行文件传输时，FTP的客户服务器之间要建立**两个并行的TCP连接**：**控制连接**和**数据连接**
- **控制连接**在整个会话期间**一直打开**，FTP客户所发出的传送请求，通过控制连接发送给服务器端的控制进程
- **数据连接**用来传输文件

**数据连接端口**：
- **主动方式**：使用TCP **20端口**
- **被动方式**：由服务器和客户端**自行协商决定**（端口>1024）

**FTP传输模式**：
- **文本模式**：ASCII模式，以文本序列传输数据
- **二进制模式**：Binary模式，以二进制序列传输数据

**简单文件传输协议TFTP**：容易实现，面向小文件，适用于UDP环境

### 电子邮件系统的补充

**电子邮件的信息格式**：信封（abc@163）+ 内容（首部、主体）

**邮件服务器**既可以当SMTP客户也可以当SMTP服务器

**SMTP的补充**：
- SMTP规定了在两个相互通信的SMTP进程之间应如何交换信息
- 负责发送邮件的SMTP进程就是SMTP客户，负责接收邮件的进程就是SMTP服务器
- SMTP使用TCP连接，C/S模式，端口号25

**SMTP通信的三个阶段**：连接建立、邮件传送、连接释放

**SMTP的缺点**：
1. SMTP不能传送可执行文件或者其他二进制对象
2. SMTP仅限于传送7位ASCII码，不能传送其他非英语国家的文字
3. SMTP服务器会拒绝超过一定长度的邮件

**通用因特网邮件扩充MIME**：在SMTP基础上扩展，通过Base64等编码将二进制转为ASCII，支持非ASCII内容（图片、音频、视频等附件）

**POP3协议的补充**：
- POP3使用TCP连接，C/S模式，端口号110
- **工作方式**：①下载并保留（在服务器） ②下载并删除

**IMAP协议的补充**：
- IMAP协议比POP协议复杂
- 当用户PC上的IMAP客户程序打开IMAP服务器的邮箱时，用户可以看到邮箱的首部，若用户需要打开某个邮件，该邮件才上传到用户的计算机上
- IMAP可以让用户在不同的地方使用不同的计算机随时上网阅读处理邮件，还允许只读取邮件中的某一个部分
- 先看正文，有WiFi的时候再下载附件

### 万维网WWW的补充

**万维网组成**：
- **统一资源定位符URL**：URL = <协议>://<主机>:<端口>/<路径>，指向万维网上的某个特定资源
  - 端口、路径可以省去，因为有默认值，如：Web服务器默认80端口，默认首页index.html
- **超文本传输协议HTTP**：应用层，基于TCP。HTTP协议定义了"HTTP请求报文"、"HTTP响应报文"
- **超文本标记语言HTML**：一种文档结构的标记语言，使用一些约定的标记对页面上的各种信息、格式进行描述

**浏览器访问一个网页的过程**：
1. 用户输入网址（域名）
2. 浏览器通过DNS服务器查询域名对应的IP地址（浏览器会将查询结果"域名→IP地址"缓存在本地）
3. 浏览器与<Web服务器IP地址 : 80端口>建立TCP连接
4. 浏览器会在握手3，携带HTTP请求报文（指明要访问哪个html网页）
5. 服务器返回HTTP响应报文（携带html报文）
6. html引用了其他**n个元素**，还需要**n组**HTTP请求&响应（**持续连接、非持续连接**工作方式有所区别）

**非持续连接**：每建立一次TCP连接，仅完成一组HTTP请求&响应

**重要结论**：
- HTTP请求报文通常很小，因此通常不考虑TCP报文段传输时延
- HTTP响应报文通常较大，因此通常要考虑TCP报文段传输时延
- **HTTP/1.0默认非持续连接**（close）
- **HTTP/1.1默认持续方式连接**（除非在HTTP请求报文中设置参数）（keep-alive）

**HTTP请求方法**：
- **HEAD**：可以用于询问是否有资源
- **账号密码**：POST

**Cookie的作用**：
- 当客户端访问服务器时，服务器生成Cookie识别码，并存储在服务器，相当于一个临时身份证
- 在HTTP请求和HTTP响应报文中都有Cookie
- **Cookie产生于服务器，存储于客户主机**

**计算公式**：
- **非持续连接**：请求一个文件时间 = 2RTT + 文件传输T
- **持续+流水**：请求一个页面时间 = 2RTT + nt
- **持续+非流水**：请求一个页面时间 = 1RTT + nRTT + nt（建立TCP、每个对象的延迟）

### 熟知端口号汇总

| FTP控制 | FTP数据 | TELNET | SMTP | DNS | DHCP服务 | DHCP客户 | TFTP | HTTP | POP3 | SNMP |
|---------|---------|--------|------|-----|----------|----------|------|------|------|------|
| 21 | 20 | 23 | 25 | 53 | 67 | 68 | 69 | 80 | 110 | 161 |

| TCP | UDP | IPv6 | OSPF |
|-----|-----|------|------|
| 6 | 17 | 41 | 89 |
